# A1: sm_its(cnci) errors informatively when no impact is available

    Code
      sm_its(corpus, intervention_year = 2020, outcome = "cnci")
    Condition
      Error:
      ! Outcome "cnci" has no usable impact data in this corpus.
      i Looked for, in order: works$cnci, corpus$metrics, and works$cited_by_count.
      i Populate `works$cnci`, a `corpus$metrics` table, or `works$cited_by_count`; enrich citations (e.g. `sm_enrich_opencitations()`) or supply impact via `sm_metric_summary()`.

