% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/journal-index.R
\name{sm_journal_in_index}
\alias{sm_journal_in_index}
\title{Verify journal source coverage against an index by ISSN}
\usage{
sm_journal_in_index(
  issn,
  index = c("wos", "scopus", "doaj"),
  reference_list = NULL,
  call = rlang::caller_env()
)
}
\arguments{
\item{issn}{Character vector of ISSNs to check. Hyphenation and case are
normalised automatically (e.g. \code{"15452786"} and \code{"1545-2786"} are
equivalent; a lowercase \code{x} check digit is upper-cased).}

\item{index}{Which index the master list represents: \code{"wos"} (default),
\code{"scopus"}, or \code{"doaj"}. This is a label echoed in the output; the actual
matching is performed entirely against \code{reference_list}.}

\item{reference_list}{A data frame master list. Expected columns (matched
case-insensitively):
\describe{
\item{title}{Journal title (aliases: \code{title}, \code{journal},
\code{journal_title}, \code{source_title}, \code{display_name}).}
\item{ISSN columns}{One or more of \code{issn}, \code{issn_print}, \code{print_issn},
\code{issn_electronic}, \code{issn_online}, \code{eissn}, \code{issn_l}. Columns whose
name indicates "electronic"/"online"/"eissn" are reported as
\code{"electronic"}; print columns as \code{"print"}; a bare \code{issn}/\code{issn_l}
column as \code{"unknown"}.}
}
A small synthetic example list ships at
\code{system.file("extdata", "example_journal_index.csv", package = "scimapR")}.}

\item{call}{Caller environment for error reporting.}
}
\value{
A tibble with one row per input \code{issn} and columns:
\code{issn} (the normalised input), \code{index}, \code{in_index} (logical),
\code{matched_title} (character or \code{NA}), \code{matched_issn_type}
(\code{"print"}/\code{"electronic"}/\code{"unknown"}/\code{NA}). Type-stable: an empty \code{issn}
vector returns a 0-row tibble with these columns.
}
\description{
Checks whether each supplied ISSN appears in a journal master list (e.g. the
Web of Science Master Journal List, a Scopus source list, or a DOAJ export).
Matching is by normalised ISSN and checks both print and electronic ISSNs.

This function is deliberately offline and data-driven: it never scrapes or
hardcodes proprietary lists. You supply the master list via \code{reference_list},
so the function is fully testable and reproducible.
}
\examples{
ref <- utils::read.csv(
  system.file("extdata", "example_journal_index.csv", package = "scimapR"),
  stringsAsFactors = FALSE
)
sm_journal_in_index(c("1078-8956", "1546-170X", "9999-9999"),
                    index = "doaj", reference_list = ref)
}
\seealso{
\code{\link[=sm_coverage_audit]{sm_coverage_audit()}}

Other coverage:
\code{\link[=sm_coverage_audit]{sm_coverage_audit()}},
\code{\link[=sm_reconcile]{sm_reconcile()}}
}
\concept{coverage}
