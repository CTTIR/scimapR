% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/affiliation.R
\name{sm_affiliation_match}
\alias{sm_affiliation_match}
\title{Match author affiliations to institutions}
\usage{
sm_affiliation_match(
  corpus,
  patterns = sm_affiliation_dict,
  fields = NULL,
  email_domain_fallback = TRUE,
  call = rlang::caller_env()
)
}
\arguments{
\item{corpus}{An \code{sm_corpus} object.}

\item{patterns}{A dictionary of institution name variants. Either:
\itemize{
\item a named list mapping each canonical institution name to a
character vector of case-insensitive regex variant patterns, or
\item a data frame with columns \code{institution}, \code{pattern}, and an
optional \code{email_domain} (the form of the bundled
\link{sm_affiliation_dict}).
}
Defaults to \link{sm_affiliation_dict}.}

\item{fields}{Character vector of \code{authorships} columns to search for
affiliation text. Defaults to \code{"raw_affiliation"} (plus \code{"email"} if such
a column exists). Values across multiple fields are concatenated per row.}

\item{email_domain_fallback}{Logical; if \code{TRUE} (default) and an authorship
has no pattern match but does have an email address (in an \code{email} column),
match the email's domain against the dictionary's \code{email_domain} entries.}

\item{call}{Caller environment for error reporting.}
}
\value{
The \code{corpus} with its \code{authorships} table gaining (or having
updated) two columns:
\describe{
\item{institution_match}{Canonical institution name, or \code{NA}.}
\item{match_method}{\code{"pattern"}, \code{"email_domain"}, or \code{"none"}.}
}
Type-stable: a corpus with no authorships is returned unchanged with the
two columns present and 0 rows.
}
\description{
A tested, extensible institution matcher for the \code{authorships} table. It
tags each authorship with the institution it belongs to, using a dictionary
of name variants (multilingual and synonym-aware) and an optional
email-domain fallback for records whose affiliation string is missing.

Because the matcher operates per authorship row, it naturally handles
secondary / multiple affiliations per author (each authorship row is matched
independently).
}
\details{
To extend the dictionary, append rows to \link{sm_affiliation_dict} (or build your
own data frame with the same columns) and pass it as \code{patterns}. For example
\code{rbind(sm_affiliation_dict, tibble::tibble(institution = "My Uni", pattern = "my university", email_domain = "myuni.edu"))}.
}
\examples{
corpus <- sm_example_corpus(n_works = 5, n_authors = 5)
corpus$authorships$raw_affiliation[1] <- "Bundeswehrkrankenhaus Berlin"
matched <- sm_affiliation_match(corpus)
matched$authorships$institution_match[1]
}
\seealso{
\code{\link[=sm_attribute_institution]{sm_attribute_institution()}}, \link{sm_affiliation_dict}

Other affiliation:
\code{\link[=sm_attribute_institution]{sm_attribute_institution()}}
}
\concept{affiliation}
