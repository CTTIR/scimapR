% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/its.R
\name{sm_its}
\alias{sm_its}
\alias{print.sm_its}
\alias{summary.sm_its}
\alias{autoplot.sm_its}
\title{Interrupted time series for a corpus outcome}
\usage{
sm_its(
  corpus,
  intervention_year,
  outcome = c("count", "share_q1", "cnci", "leadership"),
  family = NULL,
  lag = NULL,
  call = rlang::caller_env()
)

\method{print}{sm_its}(x, ...)

\method{summary}{sm_its}(object, ...)

\method{autoplot}{sm_its}(object, ...)
}
\arguments{
\item{corpus}{An \code{sm_corpus}.}

\item{intervention_year}{Integer year at which the intervention takes effect.}

\item{outcome}{One of \code{"count"} (works per year), \code{"share_q1"} (share of
Q1-journal works; needs a \code{quartile} column), \code{"cnci"} (mean
field-normalised citation impact per year), \code{"leadership"} (share of works
with a corresponding/leadership author).}

\item{family}{Optional GLM family (a \code{family} object, generator function, or
name). Defaults to Poisson for \code{"count"} and Gaussian otherwise; document
your choice when overriding.}

\item{lag}{Citation-maturity lag in years (default \code{2}). For citation-based
outcomes (\code{"cnci"}), the most recent \code{lag} years are citation-immature and
are excluded from the fit; this is reported in the print method.}

\item{call}{Caller environment for error reporting.}

\item{x}{An \code{sm_its} object.}

\item{...}{Ignored.}

\item{object}{An \code{sm_its} object.}
}
\value{
An \code{sm_its} S3 object with components: \code{model} (the fitted \code{glm}),
\code{coefficients} (tidy tibble: \code{term}, \code{estimate}, \code{std.error}, \code{conf.low},
\code{conf.high}, \code{statistic}, \code{p.value}), \code{series} (tibble: \code{year},
\code{observed}, \code{fitted}, \code{counterfactual}), and metadata (\code{outcome},
\code{intervention_year}, \code{family}, \code{lag}, \code{provisional_years}).

\code{print} returns \code{x} invisibly.

\code{summary} returns the tidy coefficient tibble.

\code{autoplot} returns a \code{ggplot}.
}
\description{
A turnkey interrupted time series (ITS): aggregates the chosen \code{outcome} to a
yearly series, fits a segmented regression with a level shift and a slope
change at \code{intervention_year}, and derives the counterfactual (the
pre-intervention trend projected forward).
}
\examples{
corpus <- sm_example_corpus(n_works = 200, seed = 1)
its <- sm_its(corpus, intervention_year = 2020, outcome = "count")
its
\donttest{
ggplot2::autoplot(its)
}
}
\seealso{
\code{\link[=sm_did]{sm_did()}}, \code{\link[=sm_citation_maturity]{sm_citation_maturity()}}

Other causal:
\code{\link[=sm_did]{sm_did()}},
\code{\link[=sm_synth]{sm_synth()}}
}
\concept{causal}
