% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/network-coword.R
\name{sm_network_coword}
\alias{sm_network_coword}
\title{Build a co-word (co-occurrence) network}
\usage{
sm_network_coword(
  corpus,
  field = c("concepts", "title", "abstract", "keywords"),
  ngram = 1L,
  min_freq = 5L,
  stopwords = NULL,
  call = rlang::caller_env()
)
}
\arguments{
\item{corpus}{An \link{sm_corpus} object.}

\item{field}{Character; the text field to extract terms from. One of
\code{"concepts"} (default, uses \code{corpus$concepts}), \code{"title"}, \code{"abstract"},
or \code{"keywords"}.}

\item{ngram}{Integer; for free-text fields (\code{"title"}, \code{"abstract"}), the
n-gram size to tokenise into. Defaults to \code{1L} (unigrams).}

\item{min_freq}{Integer; minimum document frequency a term must have to be
included. Defaults to \code{5L}.}

\item{stopwords}{Character vector of additional stopwords to remove when
tokenising free text. Combined with \link[tidytext:stop_words]{tidytext::stop_words}. Set to
\code{NULL} (default) for no additional stopwords.}

\item{call}{Caller environment for error reporting.}
}
\value{
A \link[tidygraph:tbl_graph]{tidygraph::tbl_graph} object (undirected). Nodes carry \code{name}
(the term) and \code{freq} (document frequency). Edges carry a \code{weight} column
(co-occurrence count).
}
\description{
Constructs an undirected co-word network. Nodes represent terms and an edge
connects two terms if they co-occur within the same work. Edge weight equals
the number of works in which both terms appear.
}
\details{
When \code{field = "concepts"}, terms come from the \code{concept_name} column in
\code{corpus$concepts} (no tokenisation needed).

When \code{field} is \code{"title"}, \code{"abstract"}, or \code{"keywords"}, the text is
tokenised with \code{\link[tidytext:unnest_tokens]{tidytext::unnest_tokens()}} using the chosen \code{ngram} size,
stopwords are removed, and terms below \code{min_freq} are dropped.

Empty input returns an empty undirected \code{tbl_graph}.
}
\examples{
corpus <- sm_example_corpus()
g <- sm_network_coword(corpus, field = "concepts", min_freq = 5L)
g
}
\seealso{
Other networks:
\code{\link[=sm_network_citation]{sm_network_citation()}},
\code{\link[=sm_network_cocitation]{sm_network_cocitation()}},
\code{\link[=sm_network_collab]{sm_network_collab()}},
\code{\link[=sm_network_coupling]{sm_network_coupling()}},
\code{\link[=sm_network_semantic]{sm_network_semantic()}}
}
\concept{networks}
