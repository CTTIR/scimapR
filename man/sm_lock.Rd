% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lock.R
\name{sm_lock}
\alias{sm_lock}
\alias{sm_unlock}
\title{Lock or unlock a corpus}
\usage{
sm_lock(corpus, reason = NULL, call = rlang::caller_env())

sm_unlock(corpus, confirm = FALSE, call = rlang::caller_env())
}
\arguments{
\item{corpus}{An \code{sm_corpus} object.}

\item{reason}{Optional character string documenting why the corpus was
locked (e.g., \code{"submitted for review"}, \code{"certificate generated"}).}

\item{call}{Caller environment for error reporting.}

\item{confirm}{Logical. Must be \code{TRUE} to actually unlock. This safeguard
prevents accidental unlocking of archived corpora.}
}
\value{
A modified \code{sm_corpus} with updated lock status.
}
\description{
Locking a corpus prevents mutation by \code{\link[=sm_refresh]{sm_refresh()}} and other modifying
operations. This is useful for archival or when you want to guarantee
reproducibility after generating a \code{\link[=sm_certificate]{sm_certificate()}}.

\code{sm_lock()} sets \code{metadata$is_locked = TRUE} with an optional reason.
\code{sm_unlock()} reverses the lock but requires explicit confirmation to
prevent accidental unlocking of archived corpora.
}
\examples{
corpus <- sm_example_corpus()
locked <- sm_lock(corpus, reason = "archival snapshot")
locked$metadata$is_locked

unlocked <- sm_unlock(locked, confirm = TRUE)
unlocked$metadata$is_locked
}
\seealso{
Other refresh:
\code{\link[=sm_refresh]{sm_refresh()}},
\code{\link[=sm_staleness]{sm_staleness()}}
}
\concept{refresh}
