% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/figure-manifest.R
\name{sm_figure_manifest}
\alias{sm_figure_manifest}
\title{Build a figure caption and alt-text manifest}
\usage{
sm_figure_manifest(
  dir,
  pattern = NULL,
  output = NULL,
  call = rlang::caller_env()
)
}
\arguments{
\item{dir}{Directory to scan.}

\item{pattern}{Optional regular expression to filter file names. Defaults to
common figure extensions (\code{png}, \code{pdf}, \code{svg}, \code{tif}, \code{tiff}, \code{jpg},
\code{jpeg}, \code{eps}).}

\item{output}{Optional path to write the manifest to. A \code{.csv} extension
writes CSV; \code{.yml}/\code{.yaml} writes YAML (requires the optional \pkg{yaml}
package).}

\item{call}{Caller environment for error reporting.}
}
\value{
A tibble with one row per figure file and columns \code{file}, \code{caption},
\code{alt_text}, \code{width}, \code{height}, \code{dpi}. Pixel dimensions / DPI are read from
the image where feasible (using the optional \pkg{magick} or \pkg{png}
packages) and are \code{NA} otherwise. Type-stable: a missing or empty directory
returns a 0-row tibble (with a \code{cli} warning), never an error.
}
\description{
Scans a directory of figures and produces a tidy manifest of captions,
alt-text, and (where readable) pixel dimensions and DPI. This replaces the
hand-maintained \code{figure_captions.R} scripts that accompany many manuscripts.

Captions and alt-text are pulled from a sidecar file if one is present
(\code{captions.csv}, \code{captions.yml}, or \code{captions.yaml} in \code{dir}), otherwise the
columns are left empty for the user to fill in.
}
\examples{
\donttest{
d <- withr::local_tempdir()
ggplot2::ggsave(file.path(d, "fig1.png"),
                ggplot2::ggplot(mtcars, ggplot2::aes(wt, mpg)) +
                  ggplot2::geom_point(),
                width = 4, height = 3, dpi = 150)
sm_figure_manifest(d)
}
}
\seealso{
Other reporting:
\code{\link[=sm_corpus_from_tables]{sm_corpus_from_tables()}}
}
\concept{reporting}
