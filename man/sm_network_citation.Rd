% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/network-citation.R
\name{sm_network_citation}
\alias{sm_network_citation}
\title{Build a direct citation network}
\usage{
sm_network_citation(corpus, call = rlang::caller_env())
}
\arguments{
\item{corpus}{An \link{sm_corpus} object. Must contain a non-empty \code{references}
table with columns \code{work_id} and \code{cited_work_id}.}

\item{call}{Caller environment for error reporting.}
}
\value{
A \link[tidygraph:tbl_graph]{tidygraph::tbl_graph} object (directed). Node data contains
\code{name} (the work ID) plus any columns from \code{corpus$works}. Edge data
contains \code{from}, \code{to}, and any additional reference metadata.
}
\description{
Constructs a directed citation network from the references table of an
\code{sm_corpus}. Each node represents a work and a directed edge runs from the
citing work to the cited work. Only works present in the corpus (i.e. both
the citing and cited work exist in \code{corpus$works}) are included as nodes
unless external references are also present via \code{cited_work_id}.
}
\details{
Empty input (zero works or zero references) returns an empty directed
\code{tbl_graph} with a single \code{name} column on nodes and no edges.
}
\examples{
corpus <- sm_example_corpus()
g <- sm_network_citation(corpus)
g
}
\seealso{
Other networks: 
\code{\link{sm_network_cocitation}()},
\code{\link{sm_network_collab}()},
\code{\link{sm_network_coupling}()},
\code{\link{sm_network_coword}()},
\code{\link{sm_network_semantic}()}
}
\concept{networks}
