% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/count.R
\name{sm_count}
\alias{sm_count}
\title{Full and fractional output / impact counting}
\usage{
sm_count(
  corpus,
  method = c("full", "fractional"),
  level = c("institution", "author", "source"),
  call = rlang::caller_env()
)
}
\arguments{
\item{corpus}{An \code{sm_corpus}.}

\item{method}{\code{"full"} (default) gives each entity credit 1 per work it
appears on. \code{"fractional"} splits each work's single unit of credit equally
among the distinct entities on that work.}

\item{level}{Entity level: \code{"institution"} (default), \code{"author"}, or
\code{"source"}.}

\item{call}{Caller environment for error reporting.}
}
\value{
A tibble, one row per entity, sorted by \code{credit} descending:
\describe{
\item{entity_id}{Entity identifier.}
\item{entity_name}{Human-readable name (falls back to \code{entity_id}).}
\item{n_works}{Number of distinct works the entity appears on (this is
the full count and is identical for both methods).}
\item{credit}{Output credit: \code{n_works} under \code{"full"}; the sum of
per-work fractional shares under \code{"fractional"}.}
\item{weighted_citations}{Citation impact attributed to the entity:
summed \code{cited_by_count} under \code{"full"}; summed fractional-share
weighted \code{cited_by_count} under \code{"fractional"}.}
}
Type-stable: an empty/inapplicable corpus returns a 0-row tibble with these
columns.
}
\description{
Attributes publication output (and citation impact) to entities using either
full or fractional counting. Reviewers routinely ask for fractional counts
so that a single multi-author / multi-institution paper is not counted in
full for every contributor.
}
\details{
The fractional rule is the standard equal-share rule: a work with \eqn{k}
distinct entities contributes \eqn{1/k} of credit (and \eqn{1/k} of its
citations) to each. For \code{level = "source"} each work has a single source, so
fractional and full counts coincide.
}
\examples{
corpus <- sm_example_corpus(n_works = 30, seed = 1)
sm_count(corpus, method = "fractional", level = "author")
}
\seealso{
\code{\link[=sm_metric_summary]{sm_metric_summary()}}

Other counting:
\code{\link[=sm_citation_maturity]{sm_citation_maturity()}},
\code{\link[=sm_metric_summary]{sm_metric_summary()}}
}
\concept{counting}
