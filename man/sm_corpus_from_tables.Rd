% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/corpus-from-tables.R
\name{sm_corpus_from_tables}
\alias{sm_corpus_from_tables}
\title{Construct an sm_corpus from a relational set of tables}
\usage{
sm_corpus_from_tables(
  tables,
  schema = NULL,
  .coerce = TRUE,
  metadata = list(),
  call = rlang::caller_env()
)
}
\arguments{
\item{tables}{A named list of data frames. Recognised names are \code{works},
\code{authors}, \code{authorships}, \code{institutions}, \code{sources}, \code{references},
\code{concepts}, \code{provenance}, \code{screening}. \code{works} is required (and must have,
or be coercible to, a \code{work_id} column; if absent, work ids are generated).}

\item{schema}{Optional named list of empty template tibbles overriding the
built-in schema (advanced use). Defaults to the standard \code{sm_corpus}
schema.}

\item{.coerce}{Logical (default \code{TRUE}); coerce supplied columns to the
schema's types. When \code{FALSE}, columns are used as-is (missing columns are
still filled with typed \code{NA}).}

\item{metadata}{Optional list of corpus-level metadata.}

\item{call}{Caller environment for error reporting.}
}
\value{
A validated \code{sm_corpus} object.
}
\description{
A documented, validating constructor that builds an \code{sm_corpus} from a named
list of data frames -- making non-OpenAlex/WoS tabular sources first-class
and side-stepping format-specific parsers. Required columns are validated
against the \code{sm_corpus} schema, column types are coerced (with informative
messages), absent optional tables are filled with correctly-typed 0-row
tibbles, and the result is validated before being returned.

This is the recommended ingestion path for arbitrary tabular sources.
}
\examples{
works <- data.frame(
  work_id = c("W1", "W2"),
  title = c("First", "Second"),
  year = c("2020", "2021"),   # character -> coerced to integer
  doi = c("10.1/a", "10.1/b")
)
authorships <- data.frame(
  work_id = c("W1", "W1", "W2"),
  author_id = c("A1", "A2", "A1"),
  position = c(1, 2, 1)
)
corpus <- sm_corpus_from_tables(list(works = works,
                                     authorships = authorships))
corpus
}
\seealso{
\code{\link[=sm_corpus]{sm_corpus()}}, \code{\link[=as_sm_corpus]{as_sm_corpus()}}

Other reporting:
\code{\link[=sm_figure_manifest]{sm_figure_manifest()}}

Other corpus:
\code{\link[=as_sm_corpus]{as_sm_corpus()}},
\code{\link[=is_sm_corpus]{is_sm_corpus()}},
\code{\link[=sm_bind_corpora]{sm_bind_corpora()}},
\code{\link[=sm_build_corpus]{sm_build_corpus()}},
\code{\link[=sm_corpus]{sm_corpus()}},
\code{\link[=sm_dedupe]{sm_dedupe()}},
\code{\link[=sm_materialise]{sm_materialise()}},
\code{\link[=sm_save_corpus]{sm_save_corpus()}},
\code{\link[=sm_validate]{sm_validate()}},
\code{\link[=validate_sm_corpus]{validate_sm_corpus()}}
}
\concept{corpus}
\concept{reporting}
