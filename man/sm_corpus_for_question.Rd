% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/question-build.R
\name{sm_corpus_for_question}
\alias{sm_corpus_for_question}
\title{Build a corpus from a research question}
\usage{
sm_corpus_for_question(
  question,
  sources = c("pubmed", "openalex", "crossref"),
  n_max = 1000L,
  verbose = TRUE,
  call = rlang::caller_env()
)
}
\arguments{
\item{question}{An \code{sm_question} object created with \code{\link[=sm_question]{sm_question()}}.}

\item{sources}{Character vector of sources to query. Subset of
\code{"pubmed"}, \code{"openalex"}, \code{"crossref"}.}

\item{n_max}{Integer. Maximum total number of works to retrieve
(across all sources, before deduplication). Default \code{1000}.}

\item{verbose}{Logical. Print progress messages?}

\item{call}{Caller environment for error reporting.}
}
\value{
An \code{sm_corpus} with \code{metadata$question_id} set to the
question's content-hash ID.
}
\description{
Takes a structured \code{\link[=sm_question]{sm_question()}} object, translates its fields into
database-specific query strings, fetches results from the requested
bibliographic sources, deduplicates on DOI, and returns an \code{sm_corpus}
with \code{metadata$question_id} linked to the question.

This function requires the appropriate API-client packages to be installed
for each source (\code{openalexR} for OpenAlex, \code{rcrossref} for Crossref,
\code{rentrez} for PubMed).
}
\examples{
q <- sm_question(
  text = "Does immunotherapy improve survival in melanoma?",
  framework = "PICO",
  population = "melanoma",
  intervention = "immunotherapy",
  outcome = "survival"
)
# Would query live APIs:
# corpus <- sm_corpus_for_question(q)
}
\seealso{
Other question: 
\code{\link{is_sm_question}()},
\code{\link{sm_question}()},
\code{\link{sm_screen_against_question}()},
\code{\link{sm_screen_regex}()},
\code{\link{sm_screen_summary}()}
}
\concept{question}
