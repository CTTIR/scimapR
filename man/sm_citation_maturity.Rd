% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/citation-maturity.R
\name{sm_citation_maturity}
\alias{sm_citation_maturity}
\title{Flag citation-immature recent years}
\usage{
sm_citation_maturity(corpus, lag = 2L, call = rlang::caller_env())
}
\arguments{
\item{corpus}{An \code{sm_corpus}.}

\item{lag}{Integer number of recent years to treat as citation-immature
(default \code{2}).}

\item{call}{Caller environment for error reporting.}
}
\value{
The \code{corpus} with two logical columns added to \code{works}:
\code{citation_mature} (\code{TRUE} when the year is mature) and \code{cnci_provisional}
(the negation; \code{TRUE} when citation-based indicators should be treated as
provisional). Type-stable: an empty corpus gains the columns with 0 rows.
}
\description{
Adds citation-maturity flags to the \code{works} table so that downstream
trend and impact functions can shade or exclude recent, citation-immature
years instead of treating their (artificially low) citation counts as final.

Works published within the most recent \code{lag} years -- relative to the
corpus's "as-of" date (\code{metadata$last_refresh} if present, else the maximum
publication year) -- are flagged provisional.
}
\examples{
corpus <- sm_example_corpus(n_works = 20, seed = 1)
corpus <- sm_citation_maturity(corpus, lag = 2)
table(corpus$works$cnci_provisional)
}
\seealso{
\code{\link[=sm_its]{sm_its()}}, \code{\link[=sm_metric_fnci]{sm_metric_fnci()}}

Other counting:
\code{\link[=sm_count]{sm_count()}},
\code{\link[=sm_metric_summary]{sm_metric_summary()}}
}
\concept{counting}
