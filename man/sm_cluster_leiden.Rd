% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cluster-leiden.R
\name{sm_cluster_leiden}
\alias{sm_cluster_leiden}
\title{Leiden community detection}
\usage{
sm_cluster_leiden(
  corpus,
  network = NULL,
  resolution = 1,
  call = rlang::caller_env()
)
}
\arguments{
\item{corpus}{An \link{sm_corpus} object.}

\item{network}{A \link[tidygraph:tbl_graph]{tidygraph::tbl_graph} or \link[igraph:aaa-igraph-package]{igraph::igraph} object, or
\code{NULL}. If \code{NULL} (default), a semantic similarity network is built
via \code{\link[=sm_network_semantic]{sm_network_semantic()}}.}

\item{resolution}{Numeric; resolution parameter for the Leiden algorithm.
Higher values produce more (smaller) clusters. Defaults to \code{1.0}.}

\item{call}{Caller environment for error reporting.}
}
\value{
The input \code{corpus} with a \code{cluster_id} column added to
\code{corpus$works}.
}
\description{
Clusters works using the Leiden community detection algorithm via
\code{\link[igraph:cluster_leiden]{igraph::cluster_leiden()}}. If no network is provided, a semantic
similarity network is built automatically from embeddings.
}
\details{
The Leiden algorithm (Traag et al., 2019) is a refinement of the Louvain
algorithm that guarantees well-connected communities. It operates on
the edge weights of the network.

When \code{network = NULL}, embeddings must be present in the corpus so that
\code{\link[=sm_network_semantic]{sm_network_semantic()}} can build a k-NN graph.
}
\examples{
\donttest{
if (requireNamespace("igraph", quietly = TRUE)) {
  corpus <- sm_example_corpus(with_embeddings = TRUE)
  corpus <- sm_cluster_leiden(corpus, resolution = 1.0)
  table(corpus$works$cluster_id)
}
}
}
\seealso{
Other clustering: 
\code{\link{sm_cluster_evolution}()},
\code{\link{sm_cluster_hdbscan}()},
\code{\link{sm_cluster_kmeans}()},
\code{\link{sm_cluster_label}()}
}
\concept{clustering}
