% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/materialise.R
\name{sm_materialise}
\alias{sm_materialise}
\title{Materialise cached enrichment into a corpus}
\usage{
sm_materialise(
  corpus,
  sources,
  .by = NULL,
  overwrite = FALSE,
  call = rlang::caller_env()
)
}
\arguments{
\item{corpus}{An \code{sm_corpus}.}

\item{sources}{Either a named list whose names are corpus sub-tables
(\code{works}, \code{authors}, \code{authorships}, \code{sources}, \code{institutions},
\code{references}, \code{concepts}, ...) and whose elements are tibbles or paths to
cached \code{.rds}/\code{.parquet} files; or a single directory path containing
\verb{<table>.rds} / \verb{<table>.parquet} files.}

\item{.by}{Optional named list mapping table name to its join key column(s).
Defaults to each table's natural key (e.g. \code{works} -> \code{work_id}).}

\item{overwrite}{Logical (default \code{FALSE}). When \code{FALSE}, enrichment only
fills \code{NA} cells of overlapping columns; populated cells are never
overwritten. When \code{TRUE}, non-\code{NA} enrichment values win.}

\item{call}{Caller environment for error reporting.}
}
\value{
An updated, validated \code{sm_corpus} with the enrichment columns merged
into the relevant sub-tables. New columns are added; existing rows are
preserved (this is a column-enrichment join, not a row append).
}
\description{
Joins cached enrichment data into the matching \code{sm_corpus} sub-tibbles by
their key columns, returning an updated, schema-valid \code{sm_corpus}. This
replaces hand-written cache-to-corpus joins (which are easy to get wrong ---
e.g. a \code{bind_rows()} that coerces a \code{NULL} element to a logical column).
}
\details{
Missing keys produce a \code{cli::cli_warn} and skip that source rather than
erroring. Internally, row-binds use a type-safe helper so a \code{NULL}/empty
source never corrupts a column's type.
}
\examples{
corpus <- sm_example_corpus(n_works = 10, seed = 1)
metrics <- tibble::tibble(work_id = corpus$works$work_id,
                          cnci = runif(10, 0.5, 2))
corpus2 <- sm_materialise(corpus, sources = list(works = metrics))
"cnci" \%in\% names(corpus2$works)
}
\seealso{
\code{\link[=sm_corpus_from_tables]{sm_corpus_from_tables()}}

Other corpus:
\code{\link[=as_sm_corpus]{as_sm_corpus()}},
\code{\link[=is_sm_corpus]{is_sm_corpus()}},
\code{\link[=sm_bind_corpora]{sm_bind_corpora()}},
\code{\link[=sm_build_corpus]{sm_build_corpus()}},
\code{\link[=sm_corpus]{sm_corpus()}},
\code{\link[=sm_corpus_from_tables]{sm_corpus_from_tables()}},
\code{\link[=sm_dedupe]{sm_dedupe()}},
\code{\link[=sm_save_corpus]{sm_save_corpus()}},
\code{\link[=sm_validate]{sm_validate()}},
\code{\link[=validate_sm_corpus]{validate_sm_corpus()}}
}
\concept{corpus}
