% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/network-semantic.R
\name{sm_network_semantic}
\alias{sm_network_semantic}
\title{Build a semantic similarity network}
\usage{
sm_network_semantic(corpus, k = 10L, call = rlang::caller_env())
}
\arguments{
\item{corpus}{An \link{sm_corpus} object. Must contain an \code{embeddings} matrix
(see \code{\link[=sm_embed_works]{sm_embed_works()}}).}

\item{k}{Integer; number of nearest neighbours per work. Defaults to \code{10L}.}

\item{call}{Caller environment for error reporting.}
}
\value{
A \link[tidygraph:tbl_graph]{tidygraph::tbl_graph} object (undirected). Nodes carry \code{name}
(work ID) and columns from \code{corpus$works}. Edges carry a \code{weight} column
(cosine similarity, between 0 and 1).
}
\description{
Constructs an undirected k-nearest-neighbour network based on work
embeddings. Each work is connected to its \code{k} nearest neighbours in
embedding space (cosine similarity). The edge weight is the cosine
similarity between the connected works.
}
\details{
Embeddings must be present in \code{corpus$embeddings} (a numeric matrix with
row names matching \code{work_id}). Use \code{\link[=sm_embed_works]{sm_embed_works()}} to compute them.

The function computes cosine similarity via matrix multiplication on
L2-normalised vectors and selects the top-\code{k} neighbours for each work.

Empty input or missing embeddings returns an empty undirected \code{tbl_graph}.
}
\examples{
corpus <- sm_example_corpus(with_embeddings = TRUE)
g <- sm_network_semantic(corpus, k = 5L)
g
}
\seealso{
Other networks: 
\code{\link{sm_network_citation}()},
\code{\link{sm_network_cocitation}()},
\code{\link{sm_network_collab}()},
\code{\link{sm_network_coupling}()},
\code{\link{sm_network_coword}()}
}
\concept{networks}
