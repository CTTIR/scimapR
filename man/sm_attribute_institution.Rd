% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/affiliation.R
\name{sm_attribute_institution}
\alias{sm_attribute_institution}
\title{Attribute matched affiliations to a controlled institution vocabulary}
\usage{
sm_attribute_institution(
  corpus,
  vocabulary = c("ror", "custom"),
  ror_table = NULL,
  call = rlang::caller_env()
)
}
\arguments{
\item{corpus}{An \code{sm_corpus}. If it has no \code{institution_match} column,
\code{\link[=sm_affiliation_match]{sm_affiliation_match()}} is run first with default settings.}

\item{vocabulary}{\code{"ror"} (default) or \code{"custom"}.}

\item{ror_table}{For \code{vocabulary = "ror"}, a data frame with columns
\code{ror_id}, \code{name}, and \code{aliases} (aliases either a \verb{;}-separated string or
a list-column). Matching is case-insensitive against \code{name} and each alias,
as well as against the \code{institution_match} value. A synthetic example
ships at \code{system.file("extdata", "example_ror.csv", package = "scimapR")}.}

\item{call}{Caller environment for error reporting.}
}
\value{
The \code{corpus} with its \code{authorships} table gaining \code{institution_id}
and \code{institution_name} columns (for \code{"ror"}, \code{institution_id} holds the
ROR id). Unmatched rows keep \code{NA} -- the function never errors on
unmatched affiliations. Type-stable.
}
\description{
Rolls institution matches (from \code{\link[=sm_affiliation_match]{sm_affiliation_match()}}) up to a controlled
vocabulary, writing a normalised \code{institution_id} and \code{institution_name}
onto the \code{authorships} table. Supports a ROR-backed vocabulary (via a
user-supplied offline ROR table) or a \code{"custom"} vocabulary derived directly
from the matched institution names.
}
\examples{
ror <- utils::read.csv(
  system.file("extdata", "example_ror.csv", package = "scimapR"),
  stringsAsFactors = FALSE
)
corpus <- sm_example_corpus(n_works = 5, n_authors = 5)
corpus$authorships$raw_affiliation[1] <- "Charite Universitatsmedizin Berlin"
corpus <- sm_affiliation_match(corpus)
corpus <- sm_attribute_institution(corpus, vocabulary = "ror",
                                   ror_table = ror)
corpus$authorships$institution_name[1]
}
\seealso{
\code{\link[=sm_affiliation_match]{sm_affiliation_match()}}

Other affiliation:
\code{\link[=sm_affiliation_match]{sm_affiliation_match()}}
}
\concept{affiliation}
