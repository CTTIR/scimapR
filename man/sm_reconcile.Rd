% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reconcile.R
\name{sm_reconcile}
\alias{sm_reconcile}
\alias{print.sm_reconciliation}
\alias{summary.sm_reconciliation}
\alias{autoplot.sm_reconciliation}
\title{Reconcile two corpora by DOI and title}
\usage{
sm_reconcile(
  corpus_a,
  corpus_b,
  match = c("doi_then_title", "doi", "title"),
  threshold = 0.9,
  call = rlang::caller_env()
)

\method{print}{sm_reconciliation}(x, ...)

\method{summary}{sm_reconciliation}(object, ...)

\method{autoplot}{sm_reconciliation}(object, ...)
}
\arguments{
\item{corpus_a, corpus_b}{An \code{sm_corpus} or a data frame with DOI and/or
title columns (see \code{\link[=sm_coverage_audit]{sm_coverage_audit()}} for accepted column aliases).}

\item{match}{Matching strategy: \code{"doi_then_title"} (default), \code{"doi"}, or
\code{"title"}.}

\item{threshold}{Minimum Jaro-Winkler title similarity (\verb{[0, 1]}) to accept
a title match (default \code{0.9}).}

\item{call}{Caller environment for error reporting.}

\item{x}{An \code{sm_reconciliation} object.}

\item{...}{Ignored.}

\item{object}{An \code{sm_reconciliation} object.}
}
\value{
An \code{sm_reconciliation} S3 object with components:
\describe{
\item{in_both}{Tibble of matched pairs: \code{a_id}, \code{b_id}, \code{title},
\code{match_type}, \code{match_score}.}
\item{only_a}{Tibble of \code{corpus_a} records absent from \code{corpus_b}
(\code{id}, \code{doi}, \code{title}, \code{year}).}
\item{only_b}{Tibble of \code{corpus_b} records absent from \code{corpus_a}.}
\item{matches}{Match provenance tibble (\code{a_id}, \code{b_id}, \code{match_type},
\code{match_score}), the same shape as \code{\link[=sm_coverage_audit]{sm_coverage_audit()}}'s.}
\item{summary}{One-row tibble with counts.}
}

\code{print} returns \code{x} invisibly.

\code{summary} returns the one-row summary tibble.

\code{autoplot} returns a \code{ggplot} set-size bar chart.
}
\description{
Computes a symmetric set difference between two corpora (or coercible data
frames) using the shared DOI-then-title matching engine. This generalises
\code{\link[=sm_diff_corpora]{sm_diff_corpora()}}, which compares strictly by internal \code{work_id}:
\code{sm_reconcile()} instead matches on \emph{content} (normalised DOI with a fuzzy
title fallback), so it works across corpora from different sources that do
not share identifiers.
}
\examples{
\dontshow{if (rlang::is_installed("stringdist")) withAutoprint(\{ # examplesIf}
a <- sm_example_corpus(n_works = 20, seed = 1)
b <- sm_example_corpus(n_works = 20, seed = 1)[5:20]
rec <- sm_reconcile(a, b)
rec
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=sm_coverage_audit]{sm_coverage_audit()}}, \code{\link[=sm_diff_corpora]{sm_diff_corpora()}}

Other coverage:
\code{\link[=sm_coverage_audit]{sm_coverage_audit()}},
\code{\link[=sm_coverage_breakdowns]{sm_coverage_breakdowns()}},
\code{\link[=sm_journal_in_index]{sm_journal_in_index()}}
}
\concept{coverage}
