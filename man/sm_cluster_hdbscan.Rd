% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cluster-hdbscan.R
\name{sm_cluster_hdbscan}
\alias{sm_cluster_hdbscan}
\title{HDBSCAN clustering of works}
\usage{
sm_cluster_hdbscan(
  corpus,
  min_cluster_size = 15L,
  min_samples = NULL,
  reducer = c("umap", "pca", "none"),
  n_components = 5L,
  call = rlang::caller_env()
)
}
\arguments{
\item{corpus}{An \link{sm_corpus} object with embeddings.}

\item{min_cluster_size}{Integer; minimum cluster size for HDBSCAN. Defaults
to \code{15L}.}

\item{min_samples}{Integer or \code{NULL}; minimum number of samples in a
neighbourhood for a point to be a core point. If \code{NULL} (default), set
equal to \code{min_cluster_size}.}

\item{reducer}{Character; dimensionality reduction method to apply before
clustering. One of \code{"umap"} (default), \code{"pca"}, or \code{"none"}.}

\item{n_components}{Integer; number of dimensions to reduce to. Defaults
to \code{5L}.}

\item{call}{Caller environment for error reporting.}
}
\value{
The input \code{corpus} with a \code{cluster_id} column added to
\code{corpus$works}. Noise points (not assigned to any cluster) receive
\code{cluster_id = 0L}.
}
\description{
Clusters works using the HDBSCAN (Hierarchical Density-Based Spatial
Clustering of Applications with Noise) algorithm via \code{\link[dbscan:hdbscan]{dbscan::hdbscan()}}.
Optionally reduces embedding dimensions first with UMAP or PCA.
}
\details{
Requires embeddings in \code{corpus$embeddings}. Compute them first with
\code{\link[=sm_embed_works]{sm_embed_works()}} or load from cache with \code{\link[=sm_embed_load]{sm_embed_load()}}.

The reducer step helps HDBSCAN work in a lower-dimensional space where
density estimation is more reliable.
}
\examples{
\dontshow{if (requireNamespace("dbscan", quietly = TRUE) && requireNamespace("uwot", quietly = TRUE)) withAutoprint(\{ # examplesIf}
\donttest{
corpus <- sm_example_corpus(with_embeddings = TRUE)
corpus <- sm_cluster_hdbscan(corpus, min_cluster_size = 10L)
table(corpus$works$cluster_id)
}
\dontshow{\}) # examplesIf}
}
\seealso{
Other clustering:
\code{\link[=sm_cluster_evolution]{sm_cluster_evolution()}},
\code{\link[=sm_cluster_kmeans]{sm_cluster_kmeans()}},
\code{\link[=sm_cluster_label]{sm_cluster_label()}},
\code{\link[=sm_cluster_leiden]{sm_cluster_leiden()}}
}
\concept{clustering}
